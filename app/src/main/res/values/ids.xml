<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Keys passed to View.setTag(int, Object) must be application resource ids,
        otherwise the framework throws IllegalArgumentException.
    -->
    <item name="slideshow_bitmap_tag" type="id" />
    <!-- The blurred copy that fills the background behind a fitted photo. -->
    <item name="slideshow_backdrop_tag" type="id" />
</resources>
